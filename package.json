{
  "name": "options-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "Daily screener for defined-risk options structures over a watchlist",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "server": "npm --prefix server start",
    "web": "npm --prefix web install --include=dev",
    "test": "npm --prefix server test",

    "build": "npm --prefix server install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server start"
  }
}
